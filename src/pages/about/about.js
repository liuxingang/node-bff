import "./about.less"

import utils from '../../common/utils'
import { filter, writeFn } from '../../common/filter'

console.log(utils)
console.log(filter)

let s = 'hello node bff'
alert(`index page ${s}`)
writeFn('filter Fn')